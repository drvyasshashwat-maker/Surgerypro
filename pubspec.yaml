name: SurgeryPro

description: A Flutter project.

publish_to: 'none' # Remove this line if you would like to publish to pub.dev

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  file_picker: ^5.0.0
  flutter_markdown: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
